this code establishes a Product class with a ID, name, and price. 
then there are two constructors when an object is created.
there is a default constructor that creates an object without passing any variables.
then there is a contructor that takes in the variables to create an object.
there is also a deconstructor class that is called automatically when an object is removed and it just displays a message that says it was deleted. 

in main,
the code creates an Product productA which is a Notebook and productB which just shows the default constructur 
then it prints the details of the products
and then deconstructs the objects
