this code takes the shopper behavior csv file from kaggle and reads, parses, and displays the data from the file and saves each row as an object of the CustomerRecord class.
it first reads the data from the csv file using the split function which splits a string at the comma
loads the 10 rows from the dataset, extracts 5 columns data and saves into the CustomerRecord class using the indexes
displays info into a readable table
the output of the code is included at the bototm of the code
